# Backend
- Config : `config/connectDB.js`, seeding via `seedRoles` et `seedAdmin`.
- Routes principales : `/api/auth`.
- Lancer : `npm install`, puis `npm run dev`.
- Variables requises : `MONGO_URI`, `JWT_SECRET`, infos admin initial.